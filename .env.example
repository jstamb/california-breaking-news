# Database Configuration
# For local development, use a local PostgreSQL instance
# For Cloud Run, use Cloud SQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/california_breaking_news?schema=public"

# Site Configuration
NEXT_PUBLIC_SITE_URL="https://californiabreakingnews.com"
NEXT_PUBLIC_SITE_NAME="California Breaking News"

# API Security
# Generate a secure random string for your API key
# This key is used by n8n to authenticate when posting articles
# You can generate one with: openssl rand -hex 32
API_SECRET_KEY="your-secure-api-key-for-n8n"

# Google Verification (optional)
GOOGLE_SITE_VERIFICATION="your-google-verification-code"

# Analytics (optional)
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# Cloud Run Configuration (optional - set in Cloud Run directly)
# PORT=3000
# NODE_ENV=production
